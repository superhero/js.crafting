const Component = require('..')

class CraftingComponentChartPie extends Component
{
  schema    = __dirname + '/schema'
  template  = __dirname + '/template'

  input(options)
  {
    super.input(options)
  }
}

module.exports = CraftingComponentChartPie